"use client";

import { BookOpenText, ExternalLink, UploadCloud } from "lucide-react";
import { getPublications } from "@/lib/publication-data";

export function PublicationManager(){const publications = getPublications(); return <div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-2xl border border-[#decfbf] bg-[#fffaf3] p-5"><h2 className="font-serif text-xl font-bold text-primary">હાલના preview અંકો</h2><div className="mt-4 grid gap-3">{publications.map(p=><a key={p.id} href={p.pdfUrl} target="_blank" rel="noreferrer" className="flex items-center justify-between gap-4 rounded-xl border border-border bg-white p-4"><div className="flex items-center gap-3"><BookOpenText className="h-5 w-5 text-gold-deep"/><div><strong className="text-[13px] text-primary">{p.titleGu} • {p.editionGu}</strong><div className="text-[10px] text-muted-foreground">Legacy public PDF</div></div></div><ExternalLink className="h-4 w-4 text-muted-foreground"/></a>)}</div></div><div className="rounded-2xl border border-dashed border-[#c9ad89] bg-[#fff7e9] p-6"><UploadCloud className="h-8 w-8 text-gold-deep"/><h2 className="mt-4 font-serif text-2xl font-bold text-primary">R2 upload workspace</h2><p className="mt-3 text-[13px] leading-6 text-muted-foreground">UI intentionally does not pretend to upload files before R2 is connected. Production flow will validate PDF MIME/size server-side, write metadata to D1 and publish only after review.</p><div className="mt-5 rounded-xl bg-white p-4 text-[11px] leading-6 text-[#65574d]">Planned fields: title, month, year, cover, PDF, description, publication status.</div></div></div>}
