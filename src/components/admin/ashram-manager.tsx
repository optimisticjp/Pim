"use client";

import { Search, ShieldCheck } from "lucide-react";
import { useMemo, useState } from "react";
import { ashrams } from "@/lib/site-data";

export function AshramManager(){ const [q,setQ]=useState(""); const filtered=useMemo(()=>ashrams.filter(a=>`${a.nameGu} ${a.localityGu}`.includes(q.trim())),[q]); return <div><div className="relative mb-4 max-w-xl"><Search className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><input value={q} onChange={(e)=>setQ(e.target.value)} placeholder="શાખા શોધો" className="field pl-11" /></div><div className="overflow-x-auto rounded-2xl border border-[#decfbf] bg-[#fffaf3]"><table className="w-full min-w-[760px] text-[12px]"><thead className="bg-[#efe5d8] text-left"><tr><th className="px-4 py-3">શાખા</th><th className="px-4 py-3">પ્રદેશ</th><th className="px-4 py-3">ફોન</th><th className="px-4 py-3">ચકાસણી</th></tr></thead><tbody>{filtered.map(a=><tr key={a.id} className="border-t border-[#e7dbcd]"><td className="px-4 py-3"><strong className="text-primary">{a.nameGu}</strong><div className="mt-1 text-[10px] text-muted-foreground">{a.localityGu}</div></td><td className="px-4 py-3">{a.region}</td><td className="px-4 py-3">{a.phone ?? "—"}</td><td className="px-4 py-3">{a.verified?<span className="inline-flex items-center gap-1 text-sacred-green"><ShieldCheck className="h-3.5 w-3.5"/> legacy verified</span>:<span className="text-muted-foreground">બાકી</span>}</td></tr>)}</tbody></table></div><p className="mt-3 text-[11px] text-muted-foreground">Production CRUDમાં field-level edit, map coordinates, branch manager, timings અને image gallery ઉમેરાશે. Unverified branchમાં fake data નાખવાની મંજૂરી નહીં.</p></div>; }
