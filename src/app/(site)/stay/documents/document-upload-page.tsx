"use client";
import { useEffect, useState } from "react";
import { ShieldCheck } from "lucide-react";
import { DocumentUploadForm } from "@/app/(site)/stay/documents/document-upload-form";
type Context={request_number:string;expires_at:string;remaining_uploads:number;people:Array<{guest_id:string|null;full_name:string;uploaded_count:number}>};
export function DocumentUploadPage(){const[token,setToken]=useState("");const[ctx,setCtx]=useState<Context|null>(null);const[error,setError]=useState("");useEffect(()=>{const raw=window.location.hash.replace(/^#token=/,"").trim();setToken(raw);if(raw.length!==64){setError("આ સુરક્ષિત અપલોડ કડી માન્ય નથી.");return;}void fetch("/api/stay/documents/context",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token:raw}),cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error||"Invalid link");setCtx(b);}).catch(()=>setError("આ અપલોડ કડી અમાન્ય છે અથવા તેની સમયમર્યાદા પૂર્ણ થઈ ગઈ છે."));},[]);if(error)return <div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-center font-bold text-red-900">{error}</div>;if(!ctx)return <div className="rounded-2xl border border-border bg-white p-8 text-center text-sm text-muted-foreground">સુરક્ષિત કડી ચકાસી રહ્યા છીએ…</div>;return <div><div className="mb-5 rounded-2xl border border-[#cbdad1] bg-[#edf4ef] p-4 text-sm leading-7 text-[#34564f]"><div className="flex items-center gap-2 font-bold"><ShieldCheck className="size-5"/> ખાનગી દસ્તાવેજ અપલોડ</div><p className="mt-1">અરજી: <strong>{ctx.request_number}</strong>. ફાઇલો જાહેર gallery/media માં ક્યારેય દેખાશે નહીં. દરેક ફાઇલ મહત્તમ 5 MB.</p></div><DocumentUploadForm token={token} people={ctx.people}/></div>}
